<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clt.dao.UserDao">

    <resultMap type="com.clt.entity.User" id="UserMap">
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="idcard" column="IDcard" jdbcType="VARCHAR"/>
        <result property="tel" column="tel" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="credit" column="credit" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="VARCHAR"/>
        <result property="registerTime" column="register_time" jdbcType="TIMESTAMP"/>
        <result property="lastLoginTime" column="last_login_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          user_id, user_name, password, sex, IDcard, tel, email, credit, status, role, register_time, last_login_time
        from library.user
        where user_id = #{userId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          user_id, user_name, password, sex, IDcard, tel, email, credit, status, role, register_time, last_login_time
        from library.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
          user_id, user_name, password, sex, IDcard, tel, email, credit, status, role, register_time, last_login_time
        from library.user
        <where>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="idcard != null and idcard != ''">
                and IDcard = #{idcard}
            </if>
            <if test="tel != null and tel != ''">
                and tel = #{tel}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="credit != null">
                and credit = #{credit}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
            <if test="registerTime != null">
                and register_time = #{registerTime}
            </if>
            <if test="lastLoginTime != null">
                and last_login_time = #{lastLoginTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into library.user(user_id,user_name, password, sex, IDcard, tel, email, credit, status, role, register_time, last_login_time)
        values (#{userId},#{userName}, #{password}, #{sex}, #{idcard}, #{tel}, #{email}, #{credit}, #{status}, #{role}, #{registerTime}, #{lastLoginTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update library.user
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="idcard != null and idcard != ''">
                IDcard = #{idcard},
            </if>
            <if test="tel != null and tel != ''">
                tel = #{tel},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="credit != null">
                credit = #{credit},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            <if test="registerTime != null">
                register_time = #{registerTime},
            </if>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime},
            </if>
        </set>
        where user_id = #{userId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from library.user where user_id = #{userId}
    </delete>

</mapper>